# Demo model packaging report

Status: **pending local GPU training outputs**.

After the four model directories pass `scripts/validate_demo_model.py`, package each model twice with `scripts/package_demo_models.py` and verify the two SHA-256 checksums are identical.
