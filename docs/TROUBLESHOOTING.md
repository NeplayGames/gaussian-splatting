# Troubleshooting

* CUDA unavailable: install a supported NVIDIA driver and CUDA-compatible PyTorch locally.
* Missing submodules: run `git submodule update --init --recursive`.
* Bad checksum: delete the cached asset or rerun with `--force-download`.
* Browser cannot open: rerun with `--no-open`.
