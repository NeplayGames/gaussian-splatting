# Local SEGS Quick Start

Run `python -m tools.quickstart` after installing `environment-demo.yml`. The command checks the local GPU environment, uses the cache at `~/.cache/segs-demo`, writes outputs to `demo_output`, prints a terminal table, and generates `demo_output/report.html`.

No GitHub Actions or remote execution services are used.
